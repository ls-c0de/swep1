package b_10_1;

public class Sortieren {
	public static Pruefung2[] Sortieren(Pruefung2 a[]) {
		int i = 0;
		
		while (i < 100) {
			double x = a[i].getNote();
			double y = a[i+1].getNote();
			
			if (x < y) {
				//Pruefung temp = a[i];
			}
		}
		return a;
	}

}
